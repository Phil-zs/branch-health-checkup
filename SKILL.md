---
name: branch-health-checkup
description: 当用户提供营业部数据并要求分析经营情况时，按内置41项营业部指标及19项负责人指标生成自包含HTML体检报告；支持同类别营业部均值与各维度第一名对比，未指定报告主体时再询问。
---

# Branch Health Checkup

## 适用场景与交付边界

当用户要求分析、诊断或评估营业部经营、营收情况，并提供本地营业部数据文件时使用本 Skill。支持 `.xlsx`、`.csv` 和 `.json`，先将输入统一为内部数据模型，按固定规则生成五维分析，最终只生成一份可直接本地打开的自包含 HTML 报告。

生成器仅依赖 Node.js 22+ 及其标准库。将完整 Skill 文件夹、输入数据和外部知识库复制到其他机器即可运行，无需数据库、Excel 应用、电子表格编写库、网络连接或原工作区路径。数据文件可包含已保存结果的普通 Excel 公式，但不得依赖外部工作簿链接或宏。

## 执行流程

1. 识别报告主体和同类对标范围。
2. 读取并标准化营业部指标、负责人指标和基础信息。
3. 按数据质量规则校验输入；发现阻断问题时停止并要求补充。
4. 计算营业部五大维度和负责人五项能力。
5. 对异常维度或能力选取影响最大的 1—2 个细项指标作为证据。
6. 从独立原因措施知识库匹配可能原因和建议措施。
7. 使用正式报告模板动态绑定数据，检查 HTML 结构与图表后交付。

## 输入处理

### 报告主体与同类机构

- 只提供一个数据文件时，直接生成该营业部报告，不显示任何同类对比序列。
- 提供两个或以上文件时，先读取各机构基础信息，优先使用用户已指定的营业部；主体仍不明确时才询问。仅将类别已知、且与主体的 `branch_class` 和 `period` 均相同的其他机构作为同类机构。
- 运行 `scripts/generate_report.mjs`，主体和同类文件分别通过 `--input <subject>`、`--peer-inputs <comma-separated peer file paths>` 传入，并用 `--output-dir <directory>` 指定输出目录。CSV/JSON 缺少营业部元数据时，补充 `--branch-name <name>` 和 `--period <period>`。
- 用户明确提供知识库文件时，通过 `--knowledge-base <file.xlsx>` 传入。生成器也会识别相对于输入文件所在目录的 `../knowledge/原因措施知识库.xlsx`；这是数据包相对路径约定，不是本机绝对路径。知识库不计入营业部数据文件数量。

### 字段标准化

准备或校验输入前，先读取 [references/input-contract.md](references/input-contract.md)，并按其规则解析。营业部基础信息也是输入数据，包括营业部名称、报告期间、营业部类别、区域类型、发展阶段、经营标准、负责人姓名和负责人任职起始时间；不可用字段保留 `-` 并原样展示。基础信息只放在模板现有字段中。

## 评分与分析口径

### 营业部经营分析

按照 [references/metric-catalog.json](references/metric-catalog.json) 将输入指标名称匹配到固定 41 项目录，依据 [references/indicator-rules.md](references/indicator-rules.md) 应用权重和阈值，逐项判定绿灯、黄灯或红灯并汇总五大维度及总体健康得分。正文只分析：经营效益、资产与客群、二次业务开发、团队效能、合规与风控。

红灯维度进入核心异常主题，黄灯维度进入需关注主题，绿灯维度只在汇总中展示。每个异常维度选择 1—2 个影响最大的细项指标作为趋势证据；异常卡片中不展示细项得分、评分规则、拖累分计算或其他内部计算过程。

### 负责人能力分析

当负责人字段存在时，按 [references/leader-rules.json](references/leader-rules.json) 以指标名称精确匹配 19 项负责人指标。使用 `scripts/leader-scoring.mjs`，按指标在所属能力维度中的声明权重计算，并对五项能力使用相等的 20% 维度权重。负责人等级只使用“优秀、良好、达标、待提升、预警”，以 70 分为达标线，不使用营业部红黄绿灯逻辑；不得根据名称关键词推断归属或等级。

对每个“待提升”或“预警”能力，按 `metric weight × (100 − metric score)` 选择排名前 1—2 的细项指标，并遵循 [references/leader-analysis-rules.md](references/leader-analysis-rules.md)。19 项负责人指标任一缺失时停止并要求补充。

### 原因、措施与趋势

读取独立知识库并按 [references/knowledge-base.md](references/knowledge-base.md) 匹配。原因和措施按营业部或负责人维度及状态匹配，措施还需匹配营业部类别和经营标准。原因必须表述为待核实的假设，不能写成已确认事实。可选规则表没有匹配规则时，显示明确的规则缺口信息，不自行生成自由发挥的建议。

异常卡片的“可能原因”同时承载所选指标的实际趋势观察、有保留的归因判断和知识库原因，不单独设置趋势解读栏目。模板中涉及历史变化的板块，依据输入的本期和上期值，遵循 [references/trend-rules.md](references/trend-rules.md) 生成；不得复制示例年份或趋势结论。2026 年年度栏可代表当前期间而非全年，保留数据截止口径，不将 7 月累计值与往年全年值直接解释为同口径增减。

## 报告生成规则

### 模板、模块与视觉

以 `assets/report-template-reference.html` 为唯一 HTML 结构和视觉模板。保留其模块顺序、字段标签、间距、表格、图表和负责人板块；将计算结果绑定到现有区块，不能复制示例数据，也不能追加简化替代卡片。内部指标 ID 不得出现在用户报告中。

配色与展示细节遵循 [references/report-structure.md](references/report-structure.md#approved-color-and-summary-details)，不得替换为旧版 `assets/report-template.html` 配色或黑白布局。

模板中的完整 SVG 雷达图（包括内部 `defs/style`、未填充网格圆、轴线、标签位置和浅色主体填充）必须保留，只替换计算后的多边形坐标。无对标数据时省略对比多边形和图例，不能用 0 或模板示例值代替。校验网格为 `fill: none`，主体填充透明度为 0.10；保留模板 head 样式的高优先级配色和匹配图例颜色。

### 同类对标图表

有符合条件的同类机构时，两个雷达图显示主体、同类均值和同类第一名；均值不包含主体，同类第一名按主体与同类机构各维度最高分逐维确定，可能来自不同机构。报告中说明计算口径和实际样本数。汇总表不增加同类对标列。

无符合条件的同类机构时，所有图表仅展示主体数据，不显示均值、第一名、对比多边形或相关图例；不显示不支持的图表占位符。

### 汇总表与得分展示

- 营业部汇总表列为“维度 / 绿 / 黄 / 红 / 得分”，末行提供“合计”；数量指细项指标状态数，得分按“加权得分 / 维度满分”展示。
- 负责人汇总表列为“能力维度 / 得分 / 状态”，末行提供“综合得分”。所有得分数字（包括综合得分的“总分 / 100”）统一使用普通得分的深色文字；状态使用纯文本并保留等级颜色，不加状态徽章。
- 顶部大号综合能力分数按实际等级着色，“综合能力等级”标题保持深蓝色；“重点提升”下方显示负责人五项实际维度得分。这一行与营业部紧凑灯数行均使用灰色 9px 文字。负责人同类数值单元格不加背景色。

### 异常卡片与空栏目

- 异常卡片按 [references/report-structure.md](references/report-structure.md) 的固定顺序排列：主题名称、状态、当前值 / 阈值、带关键指标证据的可能原因、证据图表（有数据支持时）、建议措施。负责人“待提升”和“预警”卡片沿用相同顺序，使用能力等级口径。
- “可能原因：”标签及正文保持同一连续段落，不强制换行。无红灯维度时省略核心异常主题，无黄灯维度时省略需关注主题，无未达标能力时省略负责人需关注能力；对应标题和占位内容一并省略，维度汇总和诊断结论继续保留。不支持的图表直接省略，不用 `-` 占位。

### 诊断结论

营业部诊断结论为一段简洁、通顺的连续文字，按综合状态、经营异常、可取之处、解决方案的顺序组织，不使用小标题、标签、列表或强制换行。负责人结论采用同样的一段式逻辑，说明能力问题与改进措施。

两类结论只能引用计算出的状态、所选证据指标名称和知识库匹配措施，不得补写未经输入支持的原因、行动或优势。

### 指标附录

附录按模板原有形式展示全部 41 项营业部指标和全部 19 项负责人指标，保持表头一致，供核查与追溯。

负责人附录列为“方案评价指标 / 本期表现 / 同类均值 / 得分”。数值观测在原单位下计算均值；评级指标按目录分数计算均值，并在报告中说明换算口径。缺少同类观测显示 `-`，不按 0 计入。

## 数据质量与阻断条件

生成前遵循 [references/data-quality-rules.md](references/data-quality-rules.md)。

### 阻断错误

- 目录指标缺失时，列出缺失指标的 ID、名称和维度，不写 HTML，要求补充后重新运行。
- 目录指标重复、必填基础信息或表头无效、指标 ID 或必需数值无效、负责人观测值无效时，以具体错误停止处理，不写 HTML。

### 可继续处理的情况

- 目录外指标列为忽略项并继续分析，不纳入评分或 41 项附录。
- 其他质量发现属于审阅警告，不改变评分、阈值、同类资格或报告结构。

## 交付与验证

报告必须包含内联 CSS、数据和渲染 JavaScript，直接从本地文件打开，不依赖测试预览文件、外部样式表或网络资源。生成后检查模板模块顺序、字段完整性、异常栏目省略逻辑、雷达图网格和对标图例，以及自包含性。

### 运行输出

| 输出 | 时机与通道 | 用途 |
| --- | --- | --- |
| `INPUT_QUALITY=<JSON>` | HTML 写入前，stderr | 提供数据质量结果，交付前向用户说明警告类别 |
| `RUN_SUMMARY=<JSON>` | 生成成功后，stdout | 提供交付摘要 |
| `OUTPUT=<path>` | 紧随运行摘要，stdout | 指向唯一的 HTML 报告 |

运行摘要包含输入覆盖率、质量状态、五维营业部得分、负责人得分、有效同类样本数及所选 1—2 个证据指标的名称；它是交付记录，不作为额外报告文件。

### 完整演示与回归验证

完整演示时，读取营业部各指标的本期总值、线上值、线下值和上期总值、线上值、线下值，共六个值，以及全部五个年度总值；同时读取负责人的本期、上期、全部五个年度观测值和所提供的基础信息。主要异常分析仍只选影响最大的 1—2 个细项指标作为趋势证据。

修改生成器、输入适配器、规则目录或报告结构后，在 Skill 根目录运行：

```powershell
node --test tests/generate-report.test.mjs
```

该测试仅使用临时 JSON fixture，不依赖本地演示数据、Excel、数据库或网络。

## 进度沟通

在读取 41 项营业部指标、计算五维得分、分析 19 项负责人指标、计算同类均值和维度第一名、检查输入质量、生成并校验报告等关键阶段，按实际运行时长提供简短进度说明。已知有效同类样本数后说明实际数量；有 `INPUT_QUALITY` 警告时，在交付前说明警告类别及其未改变评分的事实。只汇报实际进行或已完成的工作，不虚构百分比，不重复提示。脚本日志用于排查问题，不能代替用户可见的进度说明。

## 参考资料路由

按下列场景读取对应资料；修改相关口径时也须读取，不要重复维护同一规则：

| 场景 | 读取文件 | 用途 |
| --- | --- | --- |
| 解析输入 | [references/input-contract.md](references/input-contract.md) | 字段、格式和标准化 |
| 营业部评分 | [references/indicator-rules.md](references/indicator-rules.md)、[references/metric-catalog.json](references/metric-catalog.json) | 41 项指标、权重、阈值和维度汇总 |
| 负责人评分 | [references/leader-rules.json](references/leader-rules.json) | 19 项指标、权重和能力等级 |
| 负责人异常分析 | [references/leader-analysis-rules.md](references/leader-analysis-rules.md) | 关键指标、趋势、链路和归因展示 |
| 原因措施匹配 | [references/knowledge-base.md](references/knowledge-base.md) | 知识库读取与画像匹配 |
| 历史趋势 | [references/trend-rules.md](references/trend-rules.md) | 趋势和可比性口径 |
| 报告模块或字段顺序调整 | [references/report-structure.md](references/report-structure.md) | 模块、字段、配色和展示细节 |
| 数据质量 | [references/data-quality-rules.md](references/data-quality-rules.md) | 阻断项、警告项、摘要和回归要求 |

## 不可违背的边界

不得虚构指标值、阈值、得分、部门、资源或措施；不得将模板示例数据当作输入；不得在缺失目录指标时生成部分报告；不得把负责人能力套用营业部红黄绿灯。模板模块遵循上述固定结构与空异常栏目省略规则。
